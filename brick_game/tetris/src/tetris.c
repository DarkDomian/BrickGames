#include "tetris_lib.h"

static GameInfo_t current_state;

void userInput(UserAction_t action, bool hold) {
    // go to state using callback matrix
    return void;
}

GameInfo_t updateCurrentState() {
    return current_state;
}